{
  "name": "surf-academy-platform",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["api"],
  "scripts": {
    "dev": "docker compose up --build",
    "down": "docker compose down -v",
    "logs": "docker compose logs -f",
    "api:dev": "npm --workspace api run dev",
    "api:start": "npm --workspace api run start"
  }
}
